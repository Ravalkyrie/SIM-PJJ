================================================================================
DEPLOY FAVICON FIX - QUICK COMMAND
================================================================================

MASALAH YANG DIPERBAIKI:
❌ Logo PU tidak muncul di browser tab GitHub Pages
✅ Sudah diperbaiki: path favicon diubah dari /favicon.svg ke ./favicon.svg

COMMAND DEPLOY (COPY & PASTE):
================================================================================

cd "C:\New folder\Manajemen" && git add index.html && git commit -m "Fix: Favicon tidak muncul di GitHub Pages - ubah ke relative path" && git push origin main && npm run build && npm run deploy

================================================================================

LANGKAH DEMI LANGKAH:
================================================================================

1️⃣  MASUK KE FOLDER PROJECT:
    cd "C:\New folder\Manajemen"

2️⃣  TAMBAH FILE KE GIT:
    git add index.html

3️⃣  COMMIT PERUBAHAN:
    git commit -m "Fix: Favicon tidak muncul di GitHub Pages - ubah ke relative path"

4️⃣  PUSH KE GITHUB:
    git push origin main

5️⃣  BUILD PROJECT:
    npm run build

6️⃣  DEPLOY KE GITHUB PAGES:
    npm run deploy

7️⃣  TUNGGU 2-3 MENIT untuk propagasi

8️⃣  TEST DI BROWSER:
    https://ravalkyrie.github.io/SIM-PJJ/

================================================================================

VERIFIKASI SETELAH DEPLOY:
================================================================================

✅ Logo PU muncul di browser tab
✅ Hard refresh jika perlu: Ctrl + Shift + R
✅ Clear cache jika perlu: Ctrl + Shift + Delete

PERUBAHAN YANG DILAKUKAN:
================================================================================

File: index.html (line 5)

SEBELUM:
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />

SESUDAH:
<link rel="icon" type="image/svg+xml" href="./favicon.svg" />

================================================================================
SIAP DEPLOY! COPY COMMAND DI ATAS DAN JALANKAN DI TERMINAL
================================================================================
